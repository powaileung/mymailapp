<%= form_for([@post, @post.comments.build]) do |f| %>

  <div clas="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="action">
    <%= f.submit %>
  </div>

<% end %>